apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: my-namespace
spec:
  selector:
    app: backend-app  
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000



# kubectl apply -f backend-service.yaml

